# SystemVerilog

Work lives in topic folders.

| Folder | What it is |
| --- | --- |
| `cpu/` | Tiny8 microprocessor — you write the RTL |

```bash
cd cpu
make sim
```

Read `cpu/intro.md` for how a processor works, then `cpu/README.md` for what
to build.
